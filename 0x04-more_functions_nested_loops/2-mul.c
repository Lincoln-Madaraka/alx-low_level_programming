#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @x: The first number to multiply with the second number
 * @y: The second number to be multiplied by the first number
 * Return: the result of multiplication of x parameter and y parameter
 */

int mul(int x, int y)
{
	return (x * y);
}
